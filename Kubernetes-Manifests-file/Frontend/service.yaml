apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: three-tier
spec:
  type: ClusterIP
  selector:
    role: frontend
  ports:
    - port: 3000        # 👈 Service port
      targetPort: 3000 # 👈 Container port
      protocol: TCP
 
